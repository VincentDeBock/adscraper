[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev"
  targetPort = 5173
  port = 9999

# SPA fallback so client routing / direct loads work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
